# Prevent ModemManager from probing the EG95 UART (we use PPP/chat over UART).
SUBSYSTEM=="tty", KERNEL=="ttyS0", ENV{ID_MM_DEVICE_IGNORE}="1"
SUBSYSTEM=="tty", KERNEL=="ttyAMA0", ENV{ID_MM_DEVICE_IGNORE}="1"
